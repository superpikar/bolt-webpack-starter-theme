{# index.twig #}
{% extends 'master.twig' %}

{% block body %}
  <h1>
    My Blog
  </h1>
  {% for record in records %}
  <article>
    <h4>
      <a href="{{ record.link }}">{{ record.title }}</a>
    </h4>
  </article>
  {% else %}
  <article>
    no article found
  </article>
  {% endfor %}
{% endblock %}
